const { execFile} = require('child_process');
const path = require('path');
const chalk = require('chalk');
const fs = require('fs');
const logger = require('./logger')
const env = require('./env')

module.exports = async function(dir, cmd, file){
  return Promise.resovle();
};